# Crie um programa que calcule o perímetro e área de um quadrado
l = input('Digite o valor correspondente ao lado de um quadrado: ')
p = float(l) * 4
a = float(l) * float(l)

print('perímetro:', p, '- área:', a)